<div class="hero is-primary">
  <div class="hero-head">
    <nav class="nav is-primary">
      <div class="nav-left">
        <div class="nav-item is-brand">
          {{#link-to "collections" class="title"}}
            CSV Generator
          {{/link-to}}
        </div>
      </div>
      <div class="nav-right">
        <a href="https://github.com/ConorLinehan/csv-generator" class="nav-item is-brand">
          <span class="icon">
            {{fa-icon "github"}}
          </span>
        </a>
      </div>
    </nav>
  </div>
</div>
<main>
  {{outlet}}
</main>

{{modal-target}}

{{#if interface.shouldShowProgress}}
  {{to-elsewhere
    named="modal"
    send=(hash
      body=(component "progress-modal")
    )
  }}
{{/if}}
